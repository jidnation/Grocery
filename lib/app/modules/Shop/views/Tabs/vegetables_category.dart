import 'package:flutter/material.dart';

class VegetablesView extends StatelessWidget {
  const VegetablesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class VegetablesBody extends StatelessWidget {
  const VegetablesBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class VegetablesHouse extends StatelessWidget {
  const VegetablesHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
