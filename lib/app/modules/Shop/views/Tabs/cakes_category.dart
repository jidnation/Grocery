import 'package:flutter/material.dart';

class CakesView extends StatelessWidget {
  const CakesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class CakesBody extends StatelessWidget {
  const CakesBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class CakesHouse extends StatelessWidget {
  const CakesHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
