import 'package:flutter/material.dart';

class WineView extends StatelessWidget {
  const WineView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class WineBody extends StatelessWidget {
  const WineBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class WineHouse extends StatelessWidget {
  const WineHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
