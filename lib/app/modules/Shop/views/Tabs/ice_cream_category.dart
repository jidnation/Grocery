import 'package:flutter/material.dart';

class IceCreamView extends StatelessWidget {
  const IceCreamView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class IceCreamBody extends StatelessWidget {
  const IceCreamBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class IceCreamHouse extends StatelessWidget {
  const IceCreamHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
