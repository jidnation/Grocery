import 'package:flutter/material.dart';

class HandWashView extends StatelessWidget {
  const HandWashView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class HandWashBody extends StatelessWidget {
  const HandWashBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class HandWashHouse extends StatelessWidget {
  const HandWashHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
