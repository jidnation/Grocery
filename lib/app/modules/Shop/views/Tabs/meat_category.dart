import 'package:flutter/material.dart';

class MeatView extends StatelessWidget {
  const MeatView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class MeatBody extends StatelessWidget {
  const MeatBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MeatHouse extends StatelessWidget {
  const MeatHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
