import 'package:flutter/material.dart';

class SeaFoodView extends StatelessWidget {
  const SeaFoodView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class SeaFoodBody extends StatelessWidget {
  const SeaFoodBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class SeaFoodHouse extends StatelessWidget {
  const SeaFoodHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
