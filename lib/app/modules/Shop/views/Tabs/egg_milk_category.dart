import 'package:flutter/material.dart';

class EggMilkView extends StatelessWidget {
  const EggMilkView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class EggMilkBody extends StatelessWidget {
  const EggMilkBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class EggMilkHouse extends StatelessWidget {
  const EggMilkHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
