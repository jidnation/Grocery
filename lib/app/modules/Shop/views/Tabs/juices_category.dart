import 'package:flutter/material.dart';

class JuicesView extends StatelessWidget {
  const JuicesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

class JuicesBody extends StatelessWidget {
  const JuicesBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class JuicesHouse extends StatelessWidget {
  const JuicesHouse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
